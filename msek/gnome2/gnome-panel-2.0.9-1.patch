diff -Naur gnome-panel-2.0.9.old/applets/gen_util/clock.c gnome-panel-2.0.9.new/applets/gen_util/clock.c
--- gnome-panel-2.0.9.old/applets/gen_util/clock.c	Thu Jun  6 09:31:08 2002
+++ gnome-panel-2.0.9.new/applets/gen_util/clock.c	Tue Sep 17 02:18:34 2002
@@ -14,7 +14,9 @@
 #include <dirent.h>
 #include <string.h>
 #include <time.h>
+#if !defined(__APPLE__)
 #include <langinfo.h>
+#endif
 
 #include <panel-applet.h>
 #include <panel-applet-gconf.h>
@@ -709,9 +711,14 @@
 	error = NULL;
 	cd->hourformat = panel_applet_gconf_get_int (applet, KEY_HOUR_FORMAT, &error);
 	if (error || (cd->hourformat != 12 && cd->hourformat != 24)) {
+#if !defined(__APPLE__)
 		/* if value is not valid, set it according to locale */
 		const char *am = nl_langinfo (AM_STR);
 		cd->hourformat = (am[0] == '\0') ? 24 : 12;
+#else
+		/* on Darwin, just set it to 24 because there is no nl_langinfo */
+		cd->hourformat = 24;
+#endif
 
 		if (error)
 			g_error_free (error);
diff -Naur gnome-panel-2.0.9.old/applets/gen_util/help/C/clock/clock-C.omf gnome-panel-2.0.9.new/applets/gen_util/help/C/clock/clock-C.omf
--- gnome-panel-2.0.9.old/applets/gen_util/help/C/clock/clock-C.omf	Mon Aug 26 18:43:38 2002
+++ gnome-panel-2.0.9.new/applets/gen_util/help/C/clock/clock-C.omf	Fri Sep 27 02:30:35 2002
@@ -1,4 +1,4 @@
-<?xml version="1.0" encoding="UTF-8"?>
+<?xml version="1.0"?>
 <omf>
   <resource>
     <creator>
@@ -15,19 +15,15 @@
     <title>
       Clock
     </title>
--->
+<!--
     <title>
       Clock
     </title>
 -->
-    <title>
-      Clock
-    </title>
     <date>
       2002-08-01
     </date>
-    <version identifier="2.2" date="2002-08-01" description="Updated for GNOME 2.0, program version 2.0.2"/>
-    <subject category="GNOME|Applets|Accessories"/>
+    <version identifier="2.2" date="2002-08-01" description="Updated for GNOME 2.0, program version 2.0.2"/><subject category="GNOME|Applets|Accessories"/>
     <description>
       User manual for the Clock applet. 
     </description>
@@ -35,9 +31,9 @@
       user's guide
     </type>
     <format mime="text/xml" dtd="-//OASIS//DTD DocBook XML V4.1.2//EN"/>
-    <identifier url="clock.xml"/>
+    <identifier url="/sw/share/gnome/help/clock/C/clock.xml"/>
     <language code="C"/>
     <relation seriesid="dc1bc4d4-40e7-11d6-8b82-b4eda00d7b63"/>
     <rights type="GNU FDL" license.version="1.1" holder="Sun Microsystems"/>
-   </resource>        
+  </resource>
 </omf>
diff -Naur gnome-panel-2.0.9.old/configure gnome-panel-2.0.9.new/configure
--- gnome-panel-2.0.9.old/configure	Fri Sep  6 08:44:04 2002
+++ gnome-panel-2.0.9.new/configure	Tue Sep 17 02:18:34 2002
@@ -5262,7 +5262,7 @@
     # FIXME: Relying on posixy $() will cause problems for
     #        cross-compilation, but unfortunately the echo tests do not
     #        yet detect zsh echo's removal of \ escapes.
-    archive_cmds='$nonopt $(test "x$module" = xyes && echo -bundle || echo -dynamiclib) $allow_undefined_flag -o $lib $libobjs $deplibs$linker_flags -install_name $rpath/$soname $verstring'
+    archive_cmds='$nonopt $(test x$module = xyes && echo -bundle || echo -dynamiclib) $allow_undefined_flag -o $lib $libobjs $deplibs$linker_flags $(test .$module != .yes && echo -install_name $rpath/$soname $tmp_verstring)'
     # We need to add '_' to the symbols in $export_symbols first
     #archive_expsym_cmds="$archive_cmds"' && strip -s $export_symbols'
     hardcode_direct=yes
diff -Naur gnome-panel-2.0.9.old/ltmain.sh gnome-panel-2.0.9.new/ltmain.sh
--- gnome-panel-2.0.9.old/ltmain.sh	Fri Sep  6 08:43:56 2002
+++ gnome-panel-2.0.9.new/ltmain.sh	Tue Sep 17 02:18:34 2002
@@ -2952,6 +2952,11 @@
 	if test -n "$export_symbols" && test -n "$archive_expsym_cmds"; then
 	  eval cmds=\"$archive_expsym_cmds\"
 	else
+	  if test "x$verstring" = "x0.0"; then
+	    tmp_verstring=
+	  else
+	    tmp_verstring="$verstring"
+	  fi
 	  eval cmds=\"$archive_cmds\"
 	fi
 	save_ifs="$IFS"; IFS='~'
@@ -4006,10 +4011,10 @@
 
 # Directory that this library needs to be installed in:
 libdir='$install_libdir'"
-	  if test "$installed" = no && test "$need_relink" = yes; then
-	    $echo >> $output "\
-relink_command=\"$relink_command\""
-	  fi
+#	  if test "$installed" = no && test "$need_relink" = yes; then
+#	    $echo >> $output "\
+#relink_command=\"$relink_command\""
+#	  fi
 	done
       fi
 
diff -Naur gnome-panel-2.0.9.old/omf.make gnome-panel-2.0.9.new/omf.make
--- gnome-panel-2.0.9.old/omf.make	Mon May 27 19:49:09 2002
+++ gnome-panel-2.0.9.new/omf.make	Fri Sep 27 04:06:14 2002
@@ -42,7 +42,7 @@
 	for file in $(omffile); do \
 		$(INSTALL_DATA) $$file.out $(DESTDIR)$(omf_dest_dir)/$$file; \
 	done
-	-scrollkeeper-update -p $(scrollkeeper_localstate_dir) -o $(DESTDIR)$(omf_dest_dir)
+#	-scrollkeeper-update -p $(scrollkeeper_localstate_dir) -o $(DESTDIR)$(omf_dest_dir)
 
 uninstall-local-omf:
 	-for file in $(srcdir)/*.omf; do \
