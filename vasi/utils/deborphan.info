Package: deborphan
Version: 1.5
Revision: 1

BuildDepends: gettext-dev, debfoster

Source: http://ftp.debian.org/debian/pool/main/d/%n/%n_%v.orig.tar.gz
SourceDirectory: deborphan-%v
Source-MD5: 81455f1aadbc186f88f6c74411421e96

UpdatePoMakefile: true
Patch: %n.patch
SetCFLAGS: -g
SetLIBS: -lintl
ConfigureParams: --mandir=%p/share/man

InstallScript: <<
 make install DESTDIR=%d
 install -m 0755 dpkg-rebuild %i/sbin
<<

ConfFiles: %p/var/lib/deborphan/keep
DocFiles: COPYING NEWS README THANKS
Description: Orphaned package finder
DescDetail: <<
 deborphan  finds  packages that have no packages depending on them.
 The default operation is to  search  only  within the  libs  and
 oldlibs  sections,  to  hunt  down  unused libraries.
<<
DescUsage: <<
 Try 'deborphan -a'. If it does not agree with debfoster, it's probably
 because of a corrupt status file. To fix (assuming a prefix of /sw):
	
 dpkg --clear-avail
 fink scanpackages
 apt-get update
 echo /sw/var/lib/apt/lists/*Packages | xargs -n1 dpkg --merge-avail
 dpkg-rebuild
 cp /tmp/new-status /sw/var/lib/dpkg/status
<<

License: Artistic
Maintainer: Masanori Sekino <msek@users.sourceforge.net>
Homepage: http://www.tribe.eu.org/deborphan/
