diff -Naur linc-0.5.0/config.h.in linc-new/config.h.in
--- linc-0.5.0/config.h.in	Thu Feb  7 10:49:02 2002
+++ linc-new/config.h.in	Mon Jun 10 10:38:05 2002
@@ -10,7 +10,9 @@
  * Copyright 2001, Red Hat, Inc., Ximian, Inc.,
  *                 Sun Microsystems, Inc.
  */
+#if !defined (__APPLE__)
 #undef socklen_t
+#endif
 #undef HAVE_SOCKADDR_SA_LEN
 
 /* Define if you have the `basename' function. */
diff -Naur linc-0.5.0/configure linc-new/configure
--- linc-0.5.0/configure	Mon May 27 09:42:40 2002
+++ linc-new/configure	Mon Jun 10 10:38:43 2002
@@ -4438,7 +4438,7 @@
     # FIXME: Relying on posixy $() will cause problems for
     #        cross-compilation, but unfortunately the echo tests do not
     #        yet detect zsh echo's removal of \ escapes.
-    archive_cmds='$nonopt $(test "x$module" = xyes && echo -bundle || echo -dynamiclib) $allow_undefined_flag -o $lib $libobjs $deplibs$linker_flags -install_name $rpath/$soname $verstring'
+    archive_cmds='$nonopt $(test x$module = xyes && echo -bundle || echo -dynamiclib) $allow_undefined_flag -o $lib $libobjs $deplibs$linker_flags $(test x$module = xyes || echo -install_name $rpath/$soname $verstring)'
     # We need to add '_' to the symbols in $export_symbols first
     #archive_expsym_cmds="$archive_cmds"' && strip -s $export_symbols'
     hardcode_direct=yes
diff -Naur linc-0.5.0/include/linc/linc-protocol.h linc-new/include/linc/linc-protocol.h
--- linc-0.5.0/include/linc/linc-protocol.h	Mon Apr  8 11:19:19 2002
+++ linc-new/include/linc/linc-protocol.h	Mon Jun 10 10:42:24 2002
@@ -14,6 +14,10 @@
 
 #include <glib/gmacros.h>
 
+#if defined(__APPLE__) && !defined(socklen_t)
+#  define socklen_t int
+#endif
+
 G_BEGIN_DECLS
 
 #include <linc/linc-types.h>
diff -Naur linc-0.5.0/ltmain.sh linc-new/ltmain.sh
--- linc-0.5.0/ltmain.sh	Mon May 27 09:42:29 2002
+++ linc-new/ltmain.sh	Mon Jun 10 10:36:03 2002
@@ -1750,7 +1750,7 @@
 
 	  if test "$installed" = no; then
 	    notinst_deplibs="$notinst_deplibs $lib"
-	    need_relink=yes
+	    # need_relink=yes
 	  fi
 
 	  if test -n "$old_archive_from_expsyms_cmds"; then
@@ -2924,6 +2924,11 @@
 	if test -n "$export_symbols" && test -n "$archive_expsym_cmds"; then
 	  eval cmds=\"$archive_expsym_cmds\"
 	else
+	  if test "x$verstring" = "x0.0"; then
+	    tmp_verstring=
+	  else
+	    tmp_verstring="$verstring"
+	  fi
 	  eval cmds=\"$archive_cmds\"
 	fi
 	save_ifs="$IFS"; IFS='~'
diff -Naur linc-0.5.0/src/linc-protocols.c linc-new/src/linc-protocols.c
--- linc-0.5.0/src/linc-protocols.c	Fri May 17 06:00:57 2002
+++ linc-new/src/linc-protocols.c	Mon Jun 10 10:43:18 2002
@@ -10,6 +10,7 @@
  *                 Sun Microsystems, Inc.
  */
 #include <config.h>
+#include <sys/types.h>
 #include "linc-compat.h"
 #include <dirent.h>
 #include <linc/linc-protocol.h>
@@ -17,6 +18,10 @@
 
 #include "linc-private.h"
 #include "linc-debug.h"
+
+#if defined(__APPLE__) && !defined(RES_USE_INET6)
+#  undef AF_INET6
+#endif
 
 static char linc_tmpdir [PATH_MAX] = "";
 
