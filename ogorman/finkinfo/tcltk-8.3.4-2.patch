--- tk8.3.4/unix/configure	Sun Apr 29 18:36:06 2001
@@ -2370,16 +2370,14 @@
 	    ;;
 	Rhapsody-*|Darwin-*)
 	    SHLIB_CFLAGS="-fno-common"
-	    SHLIB_LD="cc -dynamiclib \${LDFLAGS}"
-	    TCL_SHLIB_LD_EXTRAS="-compatibility_version ${TCL_MAJOR_VERSION} -current_version \${VERSION} -install_name \${LIB_RUNTIME_DIR}/\${TCL_LIB_FILE} -prebind -seg1addr a000000"
-	    SHLIB_LD_LIBS="${LIBS}"
+	    SHLIB_LD="cc -dynamiclib -flat_namespace -undefined suppress \${LDFLAGS} -compatibility_version ${TK_MAJOR_VERSION} -current_version \${VERSION} -install_name <PREFIX>/lib/\${TK_LIB_FILE}"
+	    SHLIB_LD_LIBS=""
 	    SHLIB_SUFFIX=".dylib"
 	    DL_OBJS="tclLoadDyld.o"
 	    DL_LIBS=""
-	    LDFLAGS="-prebind"
+	    LDFLAGS=""
 	    LD_SEARCH_FLAGS=""
 	    CFLAGS_OPTIMIZE="-O3"
-	    EXTRA_CFLAGS="-arch ppc -pipe"
 	    ;;
 	NEXTSTEP-*)
 	    SHLIB_CFLAGS=""
diff -uNr tcl8.3.4.orig/unix/configure tcl8.3.4/unix/configure
--- tcl8.3.4.orig/unix/configure	Sat Oct 20 09:24:15 2001
+++ tcl8.3.4/unix/configure	Thu Oct 17 23:29:54 2002
@@ -6175,6 +6173,14 @@
 else
     TCL_PACKAGE_PATH="${prefix}/lib"
 fi
+
+if test -d /System/Library/Tcl; then
+	TCL_PACKAGE_PATH="$TCL_PACKAGE_PATH /System/Library/Tcl"
+fi
+
+if test -d /System/Library/Tcl/8.3/; then
+	TCL_PACKAGE_PATH="$TCL_PACKAGE_PATH /System/Library/Tcl/8.3"
+fi	
 
 #--------------------------------------------------------------------
 #       The statements below define various symbols relating to Tcl
