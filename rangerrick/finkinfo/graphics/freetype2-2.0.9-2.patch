diff -uNbr freetype-2.0.9/builds/unix/aclocal.m4 freetype-2.0.9-new/builds/unix/aclocal.m4
--- freetype-2.0.9/builds/unix/aclocal.m4	Tue Mar  5 10:14:58 2002
+++ freetype-2.0.9-new/builds/unix/aclocal.m4	Sat Nov  9 10:35:13 2002
@@ -1602,7 +1602,7 @@
       allow_undefined_flag='-undefined suppress'
       ;;
     *) # Darwin 1.3 on
-      allow_undefined_flag='-flat_namespace -undefined suppress'
+      allow_undefined_flag=''
       ;;
     esac
     # FIXME: Relying on posixy $() will cause problems for
diff -uNbr freetype-2.0.9/builds/unix/configure freetype-2.0.9-new/builds/unix/configure
--- freetype-2.0.9/builds/unix/configure	Fri Mar  8 11:02:59 2002
+++ freetype-2.0.9-new/builds/unix/configure	Sat Nov  9 10:35:26 2002
@@ -5709,13 +5709,13 @@
       allow_undefined_flag='-undefined suppress'
       ;;
     *) # Darwin 1.3 on
-      allow_undefined_flag='-flat_namespace -undefined suppress'
+      allow_undefined_flag=''
       ;;
     esac
     # FIXME: Relying on posixy $() will cause problems for
     #        cross-compilation, but unfortunately the echo tests do not
     #        yet detect zsh echo's removal of \ escapes.
-    archive_cmds='$nonopt $(test "x$module" = xyes && echo -bundle || echo -dynamiclib) $allow_undefined_flag -o $lib $libobjs $deplibs$linker_flags -install_name $rpath/$soname $verstring'
+    archive_cmds='gcc-prebind $nonopt $(test "x$module" = xyes && echo -bundle || echo -dynamiclib) $allow_undefined_flag -o $lib $libobjs $deplibs$linker_flags -install_name $rpath/$soname $verstring'
     # We need to add '_' to the symbols in $export_symbols first
     #archive_expsym_cmds="$archive_cmds"' && strip -s $export_symbols'
     hardcode_direct=yes
diff -uNbr freetype-2.0.9/builds/unix/freetype-config.in freetype-2.0.9-new/builds/unix/freetype-config.in
--- freetype-2.0.9/builds/unix/freetype-config.in	Sat Feb  2 10:05:38 2002
+++ freetype-2.0.9-new/builds/unix/freetype-config.in	Sat Nov  9 10:34:52 2002
@@ -81,7 +81,8 @@
 if test "$echo_cflags" = "yes" ; then
  cflags="-I@includedir@/freetype2"
  if test "@includedir@" != "/usr/include" ; then
-  echo -I@includedir@ $cflags
+# echo -I@includedir@ $cflags
+  echo $cflags -I@includedir@
  else
   echo $cflags
  fi
diff -uNbr freetype-2.0.9/builds/unix/ltmain.sh freetype-2.0.9-new/builds/unix/ltmain.sh
--- freetype-2.0.9/builds/unix/ltmain.sh	Tue Mar  5 10:14:59 2002
+++ freetype-2.0.9-new/builds/unix/ltmain.sh	Sat Nov  9 10:34:52 2002
@@ -2894,6 +2894,11 @@
 	if test -n "$export_symbols" && test -n "$archive_expsym_cmds"; then
 	  eval cmds=\"$archive_expsym_cmds\"
 	else
+	  if test "x$verstring" = "x0.0"; then
+	    tmp_verstring=
+	  else
+	    tmp_verstring="$verstring"
+	  fi
 	  eval cmds=\"$archive_cmds\"
 	fi
 	save_ifs="$IFS"; IFS='~'
