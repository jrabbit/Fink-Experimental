diff -Naur epiphany-1.2.5-old/configure epiphany-1.2.5/configure
--- epiphany-1.2.5-old/configure	2004-05-03 16:31:44.000000000 -0400
+++ epiphany-1.2.5/configure	2004-06-22 10:47:22.000000000 -0400
@@ -21850,6 +21850,7 @@
  1.5a) MOZILLA_SNAPSHOT=10 ;;
  1.5b) MOZILLA_SNAPSHOT=11 ;;
   1.5) MOZILLA_SNAPSHOT=11 ;;
+1.5.1) MOZILLA_SNAPSHOT=11 ;;
  1.6a) MOZILLA_SNAPSHOT=12 ;;
  1.6b) MOZILLA_SNAPSHOT=13 ;;
   1.6) MOZILLA_SNAPSHOT=13 ;;
diff -Naur epiphany-1.2.5-old/help/C/Makefile.in epiphany-1.2.5/help/C/Makefile.in
--- epiphany-1.2.5-old/help/C/Makefile.in	2004-05-03 16:31:33.000000000 -0400
+++ epiphany-1.2.5/help/C/Makefile.in	2004-06-22 10:47:22.000000000 -0400
@@ -456,7 +456,7 @@
 	for file in $(omffile); do \
 		$(INSTALL_DATA) $$file.out $(DESTDIR)$(omf_dest_dir)/$$file; \
 	done
-	-scrollkeeper-update -p $(scrollkeeper_localstate_dir) -o $(DESTDIR)$(omf_dest_dir)
+	#-scrollkeeper-update -p $(scrollkeeper_localstate_dir) -o $(DESTDIR)$(omf_dest_dir)
 
 uninstall-local-omf:
 	-for file in $(srcdir)/*.omf; do \
@@ -464,7 +464,7 @@
 		rm -f $(omf_dest_dir)/$$basefile; \
 	done
 	-rmdir $(omf_dest_dir)
-	-scrollkeeper-update -p $(scrollkeeper_localstate_dir)
+	#-scrollkeeper-update -p $(scrollkeeper_localstate_dir)
 
 clean-local-omf:
 	-for file in $(omffile); do \
